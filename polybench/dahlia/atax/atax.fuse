let A: float[390][410];
let x: float[410];
let y: float[410];
let out_Ax: float[390];

for (let m = 0..390) {
  for (let r = 0..410){
    let mul = A[m][r] * x[r];
  } combine {
    out_Ax[m] += mul;
  }
}
---
for (let n = 0..410) {
  for (let k = 0..390) {
    let mul = A[k][n] * out_Ax[k];
  } combine {
    y[n] += mul;
  }
}